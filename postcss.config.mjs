/**
 * PostCSS configuration for Tailwind CSS v4.
 * Next.js reads this file; the @tailwindcss/postcss plugin compiles the
 * `@import 'tailwindcss'` directives in src/styles.
 */
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
